package com.corne.rainfall.ui.base

/**
 * BaseState is an interface intended to be used as a base interface for states in our application.
 * It is empty and does not enforce any extra parameters on its implementors.
 * */
interface BaseState